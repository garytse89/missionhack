The backend folder (MissionHacksServer) should be separated.
Run `npm i` and `node app.js` inside the backend to host on port 3000.

The React Native app can be run using:
`react-native run-android`.
